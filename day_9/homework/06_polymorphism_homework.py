# ---------------------------------------------------------
# HOMEWORK 6: POLYMORPHISM 🍕🔺🔵■
# Practice many classes sharing the same method name.
# ---------------------------------------------------------

# Polymorphism means "many forms".
# Different objects can respond to the same method call in their own way.


# TODO: Create a class `Shape` with a method `area` that returns 0.

class Shape:
    pass  # Replace with your code


# TODO: Create a class `Circle` that inherits from `Shape`.
# It should accept `radius` and override `area` to return 3.14 * radius * radius.




# TODO: Create a class `Square` that inherits from `Shape`.
# It should accept `side` and override `area` to return side * side.




# TODO: Create a class `Triangle` that inherits from `Shape`.
# It should accept `base` and `height` and override `area`.




# TODO: Create a class `Pizza` that does NOT inherit from Shape.
# It should accept `radius` and have an `area` method too.




# TODO: Create a list called `shapes` with one of each object above.




# TODO: Loop through the list and print each object's area.
# Notice how the same method call gives different results.




# BONUS: Write a function `print_area(shape)` that calls shape.area().
# Call it for each object in your list.
